// Placeholder for Metronic UI components
// This folder can be used for storing Metronic theme components

export class MetronicConfig {
  static sidebar = {
    defaultMinimize: false,
    scrollable: true
  };

  static header = {
    fixed: true,
    height: 60
  };

  static footer = {
    fixed: false,
    height: 60
  };
}
